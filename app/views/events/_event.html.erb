<div class="event">
  <h3><%= link_to "#{event.name}", event_path(event) %></h3>
  <h4>Happening in: <%= event.city %></h4>
  <h4>On:           <%= event.date %></h4>
  <h4>Capacity      <%= event.capacity %></h4>
</div>
